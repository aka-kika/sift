# Sift pre-launch audit — 2026-08-27

Scope: full repo at v1.10.0 (`8b973b9`). Five independent passes — dead code, unused files,
hidden bugs, duplicate logic, unnecessary abstractions — then every High/Medium bug claim
re-verified by hand against the source. Findings only; nothing was changed.

Legend: **Sev** Critical/High/Medium/Low · **Conf** how sure we are it's real.

**Status:** Tier A applied the same day (10 new tests, 148 passing) — see CHANGELOG `[Unreleased]`. Tiers B and C: in progress.

---

## A. Fix before pushing to socials

These are user-visible or data-affecting, and small.

| # | Sev | Where | What | Fix |
|---|---|---|---|---|
| A1 | High | `site/index.html:512, 530, 770, 802` | Nav badge says **v1.8.0**; three spots say "Launching Sept 2026"; FAQ says "star the GitHub repo" (repo is private). | Badge → current version (and add to `release.sh` sed); replace "Launching" with "Free during the beta"; FAQ → link `/Sift.dmg`. |
| A2 | High | `README.md:22,107`, `SECURITY.md:14` | "Download from latest release" links into the **private** GitHub repo → 404 for everyone. | Point at `https://sift.akakika.com/Sift.dmg`. |
| A3 | High | `ViewModels/AppListViewModel.swift:658-680` | `reanalyze` captures `idx`, awaits 10–90 s, then indexes `apps[idx]` — but ⌘R (`runFullScan`, allowed in `.done`) sets `apps = []`, and Uninstall shrinks `apps`. Result: index-out-of-range crash, or analysis painted on the wrong row. `enrichConcurrently` already re-looks-up by bundleID after the await; `reanalyze` doesn't. | Re-resolve `idx` by bundleID after the `await`; bail if gone. |
| A4 | High | `Models/Uninstall.swift:58` | Leftover matcher uses `name.hasPrefix(id + ".")`, so uninstalling `com.google.Chrome` **pre-checks** `com.google.Chrome.canary.plist`, `…Chrome.beta` caches, Edge Canary, JetBrains CE editions — another installed app's data, silently, in one click. | Only accept a suffix that isn't itself another installed app's bundle-ID prefix; simplest: exclude candidates that match any *other* scanned bundleID. |
| A5 | High | `Views/UninstallSheet.swift:258-272`, `Services/HomebrewService.swift:72-98` | `brew uninstall` output is never checked (`runBrew` ignores `terminationStatus`, returns `""` when brew is missing). Any failure → "Homebrew finished", the `.app` row is dropped from the sweep, and the app vanishes from the list until rescan while still installed. | Check exit status; on failure keep the bundle row and show brew's stderr. |
| A6 | Medium | `Views/SettingsView.swift:249-253` + `@AppStorage("ollamaModel")` default never persisted | Opening Settings → Models auto-selects `models.first` because the stored value is `""` for anyone on the default model. Silent model switch → every cached analysis flagged "generated with a different model". Also hits Gemini/OpenRouter defaults. | Compare against `current.isEmpty ? provider.defaultModel : current`; only overwrite when the *effective* model is absent from the list. |
| A7 | Medium | `Scripts/package_app.sh:64-65`, `Services/UpdateService.swift:26,35` | `SUFeedURL` is always written (empty string when unset); `UpdateService` treats non-nil as "has feed". Fine today (version.env sets it) — wrong on any env-override build. | Emit the four `SU*` keys only when `SPARKLE_FEED_URL` is non-empty; `isAvailable` checks non-empty string. |
| A8 | Medium | `Views/AppDetailView.swift:1230-1247` + `@Attribute(.unique) bundleID` | `record` loads once per selection. If analysis lands (record created by `CacheService.save`) while the detail is open with `record == nil`, the next cube click runs `ensureRecord()` → inserts a second `AppRecord` with `explanation: ""`; the unique attribute upserts it **over** the real one — analysis, score, marks wiped; app then skipped as "locked + empty". | `ensureRecord()` re-fetches by bundleID before inserting (use `CacheService.load`). |

## B. Worth doing this week

| # | Sev | Where | What |
|---|---|---|---|
| B1 | Medium | `Views/AppRow.swift:328-345` | Every visible row does a SwiftData fetch **and** a Keychain read per render (`existingLicenseKey`), on the main thread, on every `apps` mutation. `AppRecord.hasLicenseKey` exists for this. On a differently-signed build it's an ACL prompt per row; `syncLicenseFlags` writes `hasLicenseKey = false` on a denied read, hiding the vault row. |
| B2 | Medium | `Services/HomebrewService.swift:37` | `Dictionary(uniqueKeysWithValues:)` over punctuation-stripped cask tokens traps on `google-chrome-beta` + `google-chrome@beta`-style pairs → crash on every scan. Use `uniquingKeysWith:`. |
| B3 | Medium | `Services/UpdateChecker.swift:235-244` | Sparkle parser prefers an enclosure's `sparkle:version` (a build number) when `shortVersionString` is an item element → "Update to 2045" forever. Ignores `<sparkle:channel>` / `minimumSystemVersion` too. |
| B4 | Medium | `ViewModels/AppListViewModel.swift:513-516`, `Services/LicenseKeyStore.swift:60-66` | Legacy key migration nulls `record.licenseKey` even if `SecItemUpdate`/`SecItemAdd` failed (status ignored) — one-time flag then never retries. Only affects pre-1.6 users with plaintext keys, but it is irreversible. |
| B5 | Medium | `Services/OllamaService.swift:99-101`, `AnthropicService.swift` | Ollama never checks HTTP status → 404 "model not found" surfaces as "data couldn't be read"; Anthropic 429 says "check your key". `OpenAICompatibleService.describe(status:)` already has the right messages — share it. |
| B6 | Medium | `Scripts/build_sift2.sh` vs `package_app.sh` | 68 of 90 lines are a drifting copy (Sift2 lacks `BuildTimestamp`/`LSUIElement`, hardcodes min OS, swallows codesign errors with `2>/dev/null`). Add `PRODUCT=` to `package_app.sh`, make `build_sift2.sh` a 5-line wrapper. |
| B7 | Low | `Services/UpdateChecker.swift:9`, `AppListViewModel.swift:845-853` | Update checks: 60 s default timeout, serial, and `refreshAppLinks` fetches the same feeds again. A few dead Sparkle hosts = minutes of "Checking…". |
| B8 | Low | `Views/AppDetailView.swift:682-689, 213-219` | Stale-result guard compares `app.bundleID` to itself (`app` is a `let`), so a slow "similar apps" / size lookup writes into the newly selected app. Compare against the view model's current selection. |
| B9 | Low | `AppAuditApp.swift:52` | `try! ModelContainer` with no versioned schema: a downgrade or non-lightweight change crashes before the window; only remedy is deleting the store. At least catch and offer "reset / quit". |
| B10 | Low | `Services/UpdateChecker.swift:52-63` | Homebrew branch lacks the trailing `return .upToDate(.homebrew)` the other two have — a current cask app with a Sparkle feed reports Sparkle's verdict. Intended or not, undocumented. |

## C. Cleanup (no user impact, ~600 lines lighter)

**Dead code (~140 lines, all High confidence):**
- `Services/OllamaService.swift:190-216` — "legacy compatibility" `explain/score/bestUse/parseScore/currentModel`; no production callers. Removing it lets `WorkflowProfile` drop `languages/tools/domains/projectKeywords` + `generic()` (`Models/WorkflowProfile.swift:4-21, 58-66`) — always `[]` in the app.
- `Services/UpdateChecker.swift:133-135, 156-158` `parseAppStoreVersion/parseSparkleVersion` wrappers; `Services/UpdateService.swift:38-45` `canCheck`; `Services/HomebrewService.swift:5` `installedVersion`; `AppListViewModel.swift:36-38` single-case `ReanalyzeScope`.
- 75 `#if canImport(AppKit)` / `canImport(Sparkle)` directive lines across 14 files with six never-compiled `#else` bodies — package is macOS-14-only and `AppRow`/`AppDetailView` already use AppKit unguarded.

**Duplicate logic (highest value first):**
- Update-action helpers (`icon/title/help/a11y/perform`) copied between `Views/AppRow.swift:384-450` and `Views/AppDetailView.swift:1320-1361`, copy already drifted → `extension AppInfo.UpdateSource`.
- `AppRecord(bundleID:… explanation: "", relevanceScore: 0 …)` stub hand-built ×6–7 → `init(stub:)`; four `#Predicate` fetches that duplicate `CacheService.load`; three persistence paths (`AppRow` saves directly, bypassing `CacheService`).
- Score colour 1→5 palette in `AppRow.swift:523-532` and `AppDetailView.swift:1307-1316`; `MoneyCubeState.derive` called twice with identical args in `AppDetailView:834, 893`; `"com.kikaapp.sift2"` build-variant switch in three files → `BuildVariant`.
- Provider transport: `AnthropicService.analyze` / `.complete` are a 35-line copy differing only in `max_tokens`; `ModelsResponse` declared twice; `AnalysisHTTP.systemPrompt` re-derived inline in `OllamaService:79`; Anthropic/Ollama hardcode UserDefaults keys and default models that `AnalysisProviderKind` already centralises (cache key vs request can name different models).
- Three identical 5-way provider switches (`AppListViewModel:390, 820`, `SettingsView:238`) → one factory.
- Regex first-capture helper ×3; `ByteCountFormatter`/`RelativeDateTimeFormatter`/`yyyy-MM-dd` (one unpinned locale) inline ×2 each; clipboard + Touch-ID copy + 1.5 s flash ×3; capsule pill styling ×3 with three different opacities.

**Unnecessary abstractions:**
- `MoneyPopover` takes 23 parameters; all its draft state lives in `AppDetailView` (~180 lines) — the single biggest reason that view is 1,599 lines. Move drafts + save logic into the popover.
- `AppInfo` mirrors seven `AppRecord` flags synced by hand in two places, plus seven 3-line `setX(bundleID:value:)` setters → one `update(bundleID:_:)`.
- `LicenseKeyResolution.didMigrateLegacyValue` is always false at its three call sites (launch-time migration already ran) — delete the struct and the two branches.
- `ProviderTestStatus` re-encodes `FetchState` 1:1; `SidebarEmptyState` fans into four parallel switches; `DevModeRules`/`moneyDisabled`/`NotesChange`/`identifierCandidates` are one-expression helpers with tests; hand-written `ScanState ==`; `AppRecord.init` re-assigns 23 defaults.
- Keep-justified (checked): `AnalysisService` (once a factory exists), `AnalysisProviderKind`, `LicenseType`, `MoneyCubeState`, `SendableImage`, `SecretStoreBackend` (tests fake it), `OpenAICompatibleService.Endpoint`.

**Unused / stale files:**
- `PROJECTS_MAP.md` — zero inbound refs, lists `OpenAIService.swift` (gone) → delete.
- `docs/roadmap.md` — frozen at 1.2.0, lists shipped things as "later", credits removed Apple Intelligence → rewrite or unlink.
- `site/downloads/Sift-1.8.0.dmg` — unsigned in the appcast (Sparkle ignores it; no 1.8.0 client can reach the feed) → delete, regenerate appcast.
- `.claude/worktrees/{eloquent-elbakyan-606528, heuristic-haibt-49c334}` — 1.2 GB, both ancestors of `main`; branches `claude/app-store-license-bugs-280ee9`, `claude/button-actions-clarity-879c6e` merged → `git worktree remove`, delete branches.
- Version/claim drift: `docs/README.md` "Version 1.8.0", README badge `release-1.8.0`, README + `docs/environment.md` "no third-party dependencies" (Sparkle), `docs/ARCHITECTURE.md` (Jun 28, no Sparkle/uninstall/Gemini), `docs/privacy.md` names only Anthropic/OpenAI, `CHANGELOG.md` compare links stop at 1.8.0, `docs/RELEASE.md` cites the other cert hash than `release.sh`, `Scripts/homebrew/README.md` promises a tap that doesn't exist yet.
- `docs/superpowers/{plans,specs}` — 25 executed checklists/designs, three still referenced by ADRs → archive folder, keep the three paths.
- Root: stale `Sift-1.8.0.dmg` (ignored, delete).

---

## Verified clean
No UserDefaults key drift between writers and readers. No auth bypass in `LicenseKeyGuard`. Sparkle sign order in `package_app.sh` is correct (inside-out); only caveat: `--force` re-signing drops the XPC services' entitlements, which matters only if the app is ever sandboxed. Every resource in `Resources/` and `docs/assets/` is used. Every script is in the build chain. Prompt building is single-sourced.
